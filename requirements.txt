# Web framework
flask
flask-cors

# ML / Model
torch
transformers
sentencepiece
accelerate

# Schema linking
rapidfuzz
sentence-transformers

# SQL parsing & validation
sqlparse

# Data utilities
datasets

# Database drivers
# SQLite is built into Python — no extra driver needed.
# Install the driver for the database type(s) you want to use:
psycopg2-binary        # PostgreSQL support
mysql-connector-python # MySQL / MariaDB support

# APIs & Environment
python-dotenv
google-genai
